{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture-overlay",
  "description": "截图覆盖层监听长截图交接；初始化失败时可关闭自身",
  "windows": ["capture-overlay-*"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-close"
  ]
}
